name: Docker Build and Publish
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Build Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: boaufort/gamebackend:latest
      - name: Deploy to Docker Container
        uses: appleboy/ssh-action@master
        with:
          host: http://157.230.56.58/
          username: root
          password: Emc_1486374269_Emc
          script: |
            docker stop gamebackend || true
            docker rm gamebackend || true
            docker pull boaufort/gamebackend:latest
            docker run -p 8096:8096 --name gamebackend -d boaufort/gamebackend:latest
